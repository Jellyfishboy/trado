%h1 Listing shippings

%table
  %tr
    %th Weight
    %th Size
    %th Weight
    %th Size
    %th Name
    %th Description
    %th Price
    %th Insurance
    %th
    %th
    %th

  - @shippings.each do |shipping|
    %tr
      %td= shipping.weight
      %td= shipping.size
      %td= shipping.weight_id
      %td= shipping.size_id
      %td= shipping.name
      %td= shipping.description
      %td= shipping.price
      %td= shipping.insurance
      %td= link_to 'Show', shipping
      %td= link_to 'Edit', edit_shipping_path(shipping)
      %td= link_to 'Destroy', shipping, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Shipping', new_shipping_path
