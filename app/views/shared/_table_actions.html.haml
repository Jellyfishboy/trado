%td.table-actions
  - if show
    = link_to object do
      %i.icon-search
  - if edit
    = link_to edit_polymorphic_path(object), remote: remote_edit do
      %i.icon-pencil
  / - elsif edit && !parent_object.nil?
  /   = link_to edit_polymorphic_path([:admin, parent_object, object]), remote: remote_edit do
  /     %i.icon-pencil


  / / Update stock level
  / - if type == 1
  /   = link_to new_admin_product_sku_stock_level_path(object.product, object), remote: true do
  /     %i.icon-plus
  / - if type == 1
  /   / Edit SKU
  /   = link_to edit_admin_product_sku_path(object.product, object), remote: true do
  /     %i.icon-pencil

  / / Edit order
  / - if type == 2 && object.transactions.last.completed?
  /   = link_to edit_admin_order_path(object), remote: true do
  /     %i.icon-pencil
  / Delete record
  - if del
    = link_to object, method: :delete, data: { confirm: 'Are you sure?' } do
      %i.icon-remove
