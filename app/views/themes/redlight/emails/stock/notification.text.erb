<% content_for :title, "#{@sku.product.name} Stock Reminder" %>

Stock Reminder

You requested a reminder to be sent when 12v Premium Linear Actuator is back in stock. You can now purchase this item from our online store.

<%= category_product_url(@sku.product.category, @sku.product) %>

<%= @sku.product.name %>
<%= @sku.variants.map{|v| v.name.titleize}.join(' / ') %>
<%= Store::Price.new(price: @sku.price).single %>