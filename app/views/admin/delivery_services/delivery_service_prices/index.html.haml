- content_for :title, 'Delivery service prices'
- content_for :breadcrumb, 'Delivery service prices'

.twelvecol
  .page-header
    = link_to new_admin_delivery_service_price_path, :class => "btn btn-blue btn-large", "data-placement" => "bottom", "data-toggle" => "tooltip", title: "Add delivery service price" do
      %i.icon-plus
    %h2.widget.widget-header Delivery service prices
  - if @shippings.empty?
    %p You don't have any shippings yet.
  - else
    .table-responsive
      %table.table.table-striped.table-bordered.table-hover
        %thead
          %tr
            %th Name
            %th Description
            %th Zones
            %th Net price
            %th Gross price
            %th
        %tbody
          - @shippings.each do |shipping|
            %tr
              %td= shipping.name
              %td= shipping.description
              %td
                %ul 
                  - shipping.zones.each do |zone|
                    %li
                      = zone.name
              %td= Store::Price.new(shipping.price, 'net').format
              %td= Store::Price.new(shipping.price, 'gross').format
              = table_commands shipping, admin_shipping_path(shipping), edit_admin_shipping_path(shipping), admin_shipping_path(shipping),  0
