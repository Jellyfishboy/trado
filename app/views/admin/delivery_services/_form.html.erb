<div class="threecol">
  <h3>Delivery service details</h3>
  <p>Set the courier company name, name and description of your delivery service. </p>
</div>
<div class="ninecol last">
  <fieldset class="widget widget-content">
    <div class="form-group">
      <%= f.label :courier_name %>
      <%= f.text_field :courier_name, :class => 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <div class="form-group form-last">
      <%= f.label :description %>
      <%= f.text_area :description, :class => 'form-control', rows: 7 %>
    </div>
  </fieldset>
</div>
<div class="threecol">
  <h3>Order constraints</h3>
  <p>Limit a delivery service by the total price of an order.</p>
</div>
<div class="ninecol last">
  <fieldset class="widget widget-content">
    <div class="form-group form-field-range form-last">
      <%= f.label 'Price range' %>
      <%= f.text_field :order_price_minimum, :class => 'form-control' %>
      <div class="divider"></div>
      <%= f.text_field :order_price_maximum, :class => 'form-control' %>
    </div>
  </fieldset>
</div>
<div class="threecol">
  <h3>Tracking</h3>
  <p>Set up tracking for your delivery service. Use <b>{{consignment_number}}</b> to insert consignment number.</p>
</div>
<div class="ninecol last">
  <fieldset class="widget widget-content">
    <div class="form-group form-last">
      <%= f.label :tracking_url %>
      <%= f.text_field :tracking_url, :class => 'form-control' %>
    </div>
  </fieldset>
  
</div>
<div class="threecol">
  <h3>Countries</h3>
  <p>
    Select the countries which are available for this delivery service, or
    <a href="#" id="copy-countries" data-delivery-service-id="<%= @delivery_service.id %>">copy</a>
    an existing service country list.
  </p>
</div>
<div class="ninecol last">
  <fieldset class="widget widget-content widget-last">
    <div class="form-group form-last">
      <%= f.collection_select :country_ids, @countries, :id, :name, {}, {:class => 'chosen', multiple: true, data: {placeholder: "Select associated countries..."}} %>
    </div>
  </fieldset>
  <div class="form-action">
    <%= f.submit 'Submit', :class => 'btn btn-blue btn-large' %>
  </div>
</div>
