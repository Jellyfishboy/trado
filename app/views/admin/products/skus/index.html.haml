- content_for :title, 'Product SKUs'
- content_for :breadcrumb, 'Product SKUs'
- content_for :header, 'Product SKUs'
- content_for :subnav do
  %li= link_to 'Products', admin_products_path
  %li.current
    %a.current SKUs
  %li= link_to 'Attribute types', admin_products_skus_attribute_types_path 
- if @skus.empty?
  %p You don't have any SKUs yet.
- else
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th SKU
        %th Product ID
        %th Weight
        %th Length
        %th Thickness
        %th Attribute value
        %th Attribute type
        %th Stock
        %th Stock warning level
        %th Cost value
        %th Price
        %th
    %tbody
      - @skus.each do |sku|
        %tr
          %td= sku.sku
          %td= link_to sku.product_id, [sku.product.category, sku.product]
          %td= sku.weight
          %td= sku.length
          %td= sku.thickness
          %td= sku.attribute_value
          %td= sku.attribute_type.name
          %td= sku.stock
          %td= sku.stock_warning_level
          %td= format_currency sku.cost_value
          %td= format_currency sku.price
          = table_commands sku, admin_products_sku_path(sku), edit_admin_products_sku_path(sku), admin_products_sku_path(sku), 3

#sku-stock-modal