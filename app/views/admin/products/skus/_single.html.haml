%td= sku.code
%td= sku.weight
%td= sku.length
%td= sku.thickness
%td= sku.attribute_value
%td= sku.attribute_type.name
%td= sku.stock
%td= sku.stock_warning_level
%td= Store::Price.new(price: sku.cost_value, tax_type: 'net').single
%td= Store::Price.new(price: sku.price, tax_type: 'net').single
= table_actions [:admin, sku.product, sku], 'remote-edit', 'remote-delete'