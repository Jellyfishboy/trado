$('#sku-variants-form').modal('hide');
$('#skus').html('<%=j render "admin/products/skus/table" %>');
$('#add-sku-button').show();
soca.animation.alert(
    '#skus',
    'success',
    'sku-variant-create-alert',
    '<i class="icon-checkmark-circle"></i>Successfully created <%= pluralize(sku_count, "SKU") %>.',
    3500
)