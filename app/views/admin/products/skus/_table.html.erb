<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover table-form">
    <thead>
      <tr>
        <th>Code</th>
        <th>Variant name</th>
        <th>Variant type</th>
        <th>Stock</th>
        <th>Cost value</th>
        <th>Price</th>
        <th></th>
      </tr>
    </thead>
    <tbody id="sku-fields">
      <% @product.skus.active.each do |sku| %>
        <tr id="sku_<%= sku.id %>">
          <%= render partial: 'admin/products/skus/single', format: [:html], locals: { sku: sku } %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
