- content_for :title, 'Edit product attribute type'
- content_for :breadcrumb, 'Edit'
- breadcrumb_add 'Product attribute types', admin_attribute_types_path
.page-header
    %h1 
        Editing
        = @attribute_type.name

= render 'form'

