= form_for [:admin, @pay_type] do |f|
  - if @pay_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pay_type.errors.count, "error")} prohibited this pay_type from being saved:"
      %ul
        - @pay_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
