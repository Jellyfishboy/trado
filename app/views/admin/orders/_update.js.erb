$('#order-form').modal('hide');
trado.animation.alert(
    '.widget-header', 
    'success', 
    'order-update-alert',
    '<i class="icon-checkmark-circle"></i>Successfully updated dispatch details for Order #<%= @order.id %> to <%= @order.shipping_date.strftime("#{@order.shipping_date.day.ordinalize} %b %Y") %>.',
    5000
)