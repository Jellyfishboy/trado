<%  @accessory_price = Accessory.where('id = ?', accessory_id).pluck(:price).first ? Accessory.where('id = ?', accessory_id).pluck(:price).first : 0
    @sku = Sku.find(sku_id) %>
$('.price').html('<%= format_currency (@sku.price+@accessory_price) %>');
$('.gross_price').html('<%= gross_price (@sku.price+@accessory_price) %> inc. <%= Store::settings.tax_name %>');
